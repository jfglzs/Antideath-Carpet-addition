package io.github.jfglzs.aca.command;

public class MannequinCommand {
//    public static void  register(CommandDispatcher<CommandSourceStack> dispatcher) {
//        LiteralArgumentBuilder<CommandSourceStack> command = literal("mannequin")
//                .requires((source) -> CommandHelper.canUseCommand(source, AcaSetting.enableMannequinCommand))
//                .executes(context -> {
//                    CommandUtils.runCommand("execute as %s at @s run summon minecraft:mannequin ~ ~ ~ {profile:%s}"
//                            .formatted(context.getSource().getPlayer().getName())context.getSource())
//                    );
//                    return 1;
//                });
//
//        dispatcher.register(command);
//    }
}
